/**
 * Placeholder — manual type definitions for tables not yet in database.types.ts.
 *
 * The `untypedFrom()` helper in lib/supabase.ts centralises the single
 * `as any` cast needed for accessing these tables. Once `supabase gen types`
 * is re-run, remove `untypedFrom()` and this file.
 *
 * Missing tables: leader_todos, impact_metric_defs, app_settings
 */
export {}
