import { useLeaderHeader, useIsLeaderLayout } from '@/components/leader-layout'

/**
 * Leader reports — re-exports the shared ReportsPage,
 * but sets the leader hero header when inside the leader layout.
 */
export { default } from '@/pages/reports/index'
